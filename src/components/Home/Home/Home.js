import React from 'react';
import MainNavbar from '../../Shared/MainNavbar/MainNavbar'

const Home = () => {
    return (
        <section>
            <MainNavbar />
        </section>
    );
};

export default Home;